{
  "name": "coup-ai-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Headless Coup self-play environment and lightweight RL trainer.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "train": "node scripts/train-ppo.js",
    "train:full": "node scripts/train-ppo.js --iterations 500 --games-per-iteration 192 --players 3 --eval-stat-games 200 --eval-every 10 --out models/ppo-agent.json",
    "train:linear": "node scripts/train-rl.js",
    "evaluate": "node scripts/evaluate-rl.js",
    "evaluate:statistical": "node scripts/evaluate-vs-statistical.js",
    "verify:statistical": "node scripts/verify-statistical-baseline.js",
    "export:browser": "node scripts/export-browser-model.js",
    "smoke": "node scripts/smoke-random-games.js"
  }
}
